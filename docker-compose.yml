services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: parse_xml
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U mqc -d mqc"]
#      interval: 5s
#      timeout: 5s
#      retries: 10